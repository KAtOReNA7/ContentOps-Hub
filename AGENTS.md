# AGENTS.md

## 项目说明

本项目是 `ContentOps Hub`，用于“番茄畅听 / 番茄畅畅听”多书名运营辅助场景。品牌称呼仍待产品负责人确认，开发中优先使用中性项目名 `ContentOps Hub`。

## 项目阶段

- 项目当前处于稳定化和真实业务验收阶段。
- 默认不新增大型业务模块，不再以不断增加“阶段编号”作为主要开发方式。
- 优先处理数据风险、核心流程、错误恢复、性能、测试和文档一致性。
- 当前真实状态只维护在 `docs/CURRENT_STATUS.md`。
- 阶段流水账不得继续作为当前事实源；历史记录归档到 `docs/archive/`。
- 重要架构决策记录到独立决策文档。
- 过时文档只归档或标记失效，不直接删除。

## 工具和插件

- Codex 根据任务自行选择插件、Skills 和本地工具。
- 涉及页面、交互或用户流程时，必须使用 Browser 在修改前后验证。
- 涉及 GitHub PR、Issue、提交或 CI 时，可以使用 GitHub 插件。
- 涉及 OpenAI 接口时，优先核对官方文档或当前项目 Provider 契约。
- 涉及数据和性能时，可以使用 Data Analytics。
- 发布前或安全敏感修改时，可以使用 Codex Security。
- 普通文件和命令行足以完成时，不为了调用插件而调用插件。

## 风险操作

未经用户明确授权，不得：

- 合并 PR
- 直接推送 main
- 部署生产环境
- 修改或删除生产数据
- 删除历史业务模型
- 批量调用收费 API
- 自动执行 Image2 批量生成
- 修改真实外部平台内容

## Prisma、密钥、依赖和超时规则

1. 禁止使用 `node -e` 测试 Prisma。
2. Prisma 连接测试只能使用：
   - `npm exec -- prisma validate`
   - `npm exec -- prisma generate`
   - `npm run db:test`
3. 不要随意升级 Prisma，不要改回 Prisma 7。
4. 不要在没有说明原因的情况下修改 `prisma/schema.prisma`。
5. 不要接真实搜索 API，除非当前任务明确要求。
6. 不要接真实 OpenAI API，除非当前任务明确要求。
7. 不要把 API key 写入代码、文档、日志、页面或导出文件。
8. 如果需要新增依赖，先说明新增依赖名称和用途。
9. 每条验证命令最多等待 60 秒，超过就停止并记录 timeout。
10. 如果遇到命令卡住，不要无限排查，先保存状态并输出恢复报告。
11. 不要删除已完成代码，除非明确说明原因并得到确认。
12. 任何会写数据库的自动测试必须使用白名单隔离测试库，例如 `prisma/test-*.db`，不得写入 `prisma/dev.db`。
13. 未经用户明确授权，不得对 `prisma/dev.db` 执行 reset、seed、`deleteMany`、清空、fixture 清理或其他批量写入操作。
14. 修改数据库相关代码前，必须确认当前备份或基线状态；数据库测试完成后，必须证明 `dev.db` 的主要业务表计数和逻辑摘要未变化。
15. 测试脚本不得选择任意现有真实 Work 作为写入对象；需要写入时必须在隔离测试库中自行构造最小 fixture。

## 完成标准

每个开发任务完成后，根据修改范围运行：

- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `npm run db:test`
- 相关专项测试
- 涉及 UI 时使用 Browser 验证

任务结束必须说明：

- 修改内容
- 使用的插件和 Skills
- 验证命令
- Browser 验证页面
- 外部写操作
- 剩余风险
- 对应提交 SHA
